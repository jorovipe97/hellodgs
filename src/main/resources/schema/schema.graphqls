type Query {
    shows(titleFilter: String): [Show]
}

type Show {
    id: ID
    title: String
    releaseYear: Int
    actors: [Actor]
}

type Actor {
    name: String!
    age: Int
}
